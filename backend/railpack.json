{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "exclude": ["livekit-worker/**", "tests/**"],
  "deploy": {
    "startCommand": "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
