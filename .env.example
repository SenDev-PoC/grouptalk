# Supabase
# 두 값이 모두 있어야 실제 Supabase에 연결한다.
# 비워두면 앱은 브라우저 안의 데모 데이터로 동작한다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# LiveKit
# 토큰 응답에 url이 없을 때 사용할 기본 서버 주소
VITE_LIVEKIT_URL=

# 토큰 발급 엔드포인트.
# 비워두면 Supabase Edge Function `livekit-token`을 호출한다.
VITE_LIVEKIT_TOKEN_ENDPOINT=
